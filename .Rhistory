b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
cutoff=500
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/3,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/4,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/4,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/4,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/4,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/4,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/4,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
6000/500
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
t.test(x$counts,b$counts*100)
dim(len)
length(len)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
length(len)/cutoff
cutoff=50
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/5,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*cutoff/2,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*20,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*2000,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*500,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
6000/15
6000/50
52000/50
source('~/.active-rstudio-document', echo=TRUE)
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
b$counts*length(len)/cutoff
x$counts
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
cutoff=500
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
cutoff=30
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
cutoff=50
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
#temp code to plot histogram of each bin
x=hist(len,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3))
b.dat=sample(len,size=cutoff)
b=hist(b.dat,breaks=seq(from=range(len)[1],to=range(len)[2],l=(range(len)[2]-range(len)[1]+1)/3),plot=FALSE)
lines(b$breaks[1:59]-1,b$counts*length(len)/cutoff,lwd=2,col='red')
t.test(x$counts,b$counts*length(len)/cutoff)
3900/12
